<aura:component
  implements="lightning:actionOverride,force:hasRecordId,force:hasSObjectName"
  access="global"
>
  <!-- 仕様: Core 第3.3.2節。New／Edit 上書き。LWC は lightning:actionOverride の Aura で包む。 -->
  <aura:attribute name="formMode" type="String" default="new" />
  <aura:handler name="init" value="{!this}" action="{!c.syncMode}" />
  <aura:handler
    name="change"
    value="{!v.recordId}"
    action="{!c.syncMode}"
  />
  <div class="slds-p-around_medium">
    <c:billingAccountForm
      recordId="{!v.recordId}"
      objectApiName="{!v.sObjectName}"
      formMode="{!v.formMode}"
    />
  </div>
</aura:component>
