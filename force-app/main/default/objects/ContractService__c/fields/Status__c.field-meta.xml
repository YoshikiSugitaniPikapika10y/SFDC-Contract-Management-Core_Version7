<?xml version="1.0" encoding="UTF-8" ?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Status__c</fullName>
    <description
  >InTerm / OutOfTerm / Cancelled / SPOT。未受注は空白。</description>
    <externalId>false</externalId>
    <formula>IF(
  ISBLANK(LatestOrderedHistory__c),
  &quot;&quot;,
  IF(
    TEXT(Lifecycle__c) = &quot;Spot&quot;,
    &quot;SPOT&quot;,
    IF(
      TEXT(LatestOrderedHistory__r.Type__c) = &quot;Cancel&quot;,
      &quot;Cancelled&quot;,
      IF(
        AND(
          NOT(ISBLANK(ServiceEndDate__c)),
          TODAY() &gt; ServiceEndDate__c
        ),
        &quot;OutOfTerm&quot;,
        &quot;InTerm&quot;
      )
    )
  )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText
  >期間契約: InTerm（期間内）/ OutOfTerm（超過）/ Cancelled（解約済）。都度契約: SPOT。未受注は空白。</inlineHelpText>
    <label>ステータス</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
